<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.madcourse.NUMAD21Su_LilyBessette">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_lgb"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_lgb_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21Su_LilyBessette">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClickyActivity"></activity>
    </application>

</manifest>